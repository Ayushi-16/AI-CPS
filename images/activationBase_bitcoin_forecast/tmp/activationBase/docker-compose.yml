services:
  activation_base:
    build: .
    image: ayushi1612/activationbase_bitcoin_forecast:latest
    container_name: activationbase_bitcoin_forecast
    volumes:
       - activationbase_volume:/tmp
    command: sh -c "tail -f /dev/null"
volumes:
  activationbase_volume:
    name: ai_system
    external: true